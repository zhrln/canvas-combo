/**
 * Created by zhangrui on Fri Jan 29 2016.
 */

(function(win, lib){

    if(lib._libname_){
        return lib._libname_;
    }

    lib._libname_ = {};


}(window, window['lib'] || (window['lib'] = {})));
